# Use the same lightweight Node.js image for consistency
FROM node:18-alpine

# Set the working directory
WORKDIR /app

# Copy package manifests
COPY package*.json ./

# Install dependencies. The --force flag can help resolve
# peer dependency issues common in complex JS projects.
RUN npm install --force

# Copy the rest of the application code
COPY . .

# Expose port 3000 to the Docker network
EXPOSE 3000

# Command to start the Next.js development server
CMD ["npm", "run", "dev"]